[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Add project root to pythonpath to help with imports like 'from app import ...'
# This is often needed if tests are run from the project root and 'app' is a top-level directory.
pythonpath = .
